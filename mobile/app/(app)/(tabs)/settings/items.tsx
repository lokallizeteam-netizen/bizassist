// BizAssist_mobile
// path: app/(app)/(tabs)/settings/items.tsx

import { Redirect } from "expo-router";

export default function SettingsItemsRedirectScreen() {
	return <Redirect href='/(app)/(tabs)/settings' />;
}
