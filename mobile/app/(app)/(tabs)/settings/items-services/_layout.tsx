// BizAssist_mobile
// path: app/(app)/(tabs)/settings/items-services/_layout.tsx

import { Redirect } from "expo-router";

export default function SettingsItemsServicesDisabledLayout() {
	return <Redirect href='/(app)/(tabs)/settings' />;
}
