// BizAssist_mobile
// path: app/(app)/(tabs)/settings/items-services/index.tsx

import { Redirect } from "expo-router";

export default function SettingsItemsServicesRedirectScreen() {
	return <Redirect href='/(app)/(tabs)/settings' />;
}
